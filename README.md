# binIO
Simple utility to handle binary strings and hex encodings.
I prefer static libraries, and that's what the utility will compile to.

To build:  
`  make`  

To run tests:  
`  make test`  
`  ./test`  
